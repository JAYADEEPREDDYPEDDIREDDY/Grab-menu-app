# grab-menu

Repo structure:

- `frontend/` - React + Vite customer/admin frontend
- `server/` - Express + MongoDB backend

Run from the separated apps directly:

- Frontend: `cd frontend && npm run dev`
- Backend: `cd server && node server.js`
